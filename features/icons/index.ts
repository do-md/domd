/**
 * App icon set: one memoized component per file, filled-outline style on the
 * 1024 grid, line weight normalized to ~69 units (see table-icon.tsx).
 * Add new icons here as individual files and re-export them below.
 */
export { TableIcon } from "./table-icon";
export { ChecklistIcon } from "./checklist-icon";
